     vector<int>Series;

for(int i=0;i<=n;i++){
    
    if(i==0){
      Series.push_back(0); 
    }
    
    else if(i==1){
        Series.push_back(1);
    }
    
    else{
        Series.push_back(Series[i-1]+Series[i-2]);
    }
}

return Series;

}
